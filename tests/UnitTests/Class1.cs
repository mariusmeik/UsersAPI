namespace UnitTests
{
    public class Class1
    {
        //_refreshService.Verify(x => x.InitiateRefreshAsync(), Times.Once);

        //_currencyExchangeRateRepositoryMock.Setup(x => x.GetByNameAndDateAsync("USD", new DateTime(2022, 10, 16)))
        //                                    .Returns(Task.FromResult(firstExchangeRate));

        // Assert.That(ex.Message, Is.EqualTo("Fetched rates object was null!"));

    }
}